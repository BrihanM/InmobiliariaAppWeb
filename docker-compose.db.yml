version: '3.8'
services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: inmobiliaria
    ports:
      - "5432:5432"
    volumes:
      - inmobiliaria_db_data:/var/lib/postgresql

volumes:
  inmobiliaria_db_data:
